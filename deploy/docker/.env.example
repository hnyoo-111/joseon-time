# docker compose 용 설정 — 이 파일을 deploy/docker/.env 로 복사해 쓴다.
# (애플리케이션 시크릿이 아니라 compose 변수다. Sketchfab 토큰은 HOST_ROOT/.env 에 있다)

# 호스트에서 데이터가 놓이는 위치. 컨테이너 내부는 항상 /srv/joseon-time 이다.
HOST_ROOT=/data/joseon-time
